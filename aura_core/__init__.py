"""Aura Core — the on-device edge brain for the Aura driver-persona prototype.

Hub-and-spoke: this service owns the camera + persona + adaptive-safety policy and
broadcasts driver-state events over WebSocket to the Unity game and the React dashboard.
"""

__version__ = "0.1.0"
